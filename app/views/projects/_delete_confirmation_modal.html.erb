<dialog id="deleteProjectModal<%= project.id %>" class="modal">
  <div class="modal-box">
    <form method="dialog">
      <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">✕</button>
    </form>
    <h3 class="text-lg font-bold">Delete Project?</h3>
    <p class="py-4">Are you sure you want to delete this project? <br>
      This will delete all the related tasks and time logs.</p>
    <div class="modal-action">
      <%= button_to "Confirm", project, method: :delete, class: "btn btn-error float-right" %>
    </div>
  </div>
</dialog>
